using UnityEngine;
public class S3Atk : MonoBehaviour
{

    public int st3_damage;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
